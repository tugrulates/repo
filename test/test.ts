/**
 * Test publishing to JSR.
 *
 * ### Modules
 *
 *  -  {@link [module]}: Test module.
 *
 * @example Module example
 * ```ts
 * import { func } from "@tugrulates/test/module";
 * func();
 * ```
 *
 * @module test
 */

import { func } from "@tugrulates/test/module";

func();
