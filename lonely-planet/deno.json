{
  "name": "@tugrulates/lonely-planet",
  "version": "0.2.0",
  "exports": "./mod.ts",
  "tasks": {
    "compile": "deno compile $COMPILE_ARGS -R=$HOME/.scripts -W=$HOME/.scripts -E=HOME -N --unstable-kv --no-prompt --include=deno.json -o=../dist/ mod.ts"
  }
}
